export const selectSpaces = (state) => state.spaces.allSpaces;

export const selectSpaceDetails = (state) => state.spaces.spaceDetails;
export const selectStories = (state) => state.spaces.spaceDetails.stories;
